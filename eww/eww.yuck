(defvar hello true)
(defwindow powermenu
  ;; monitor 0 ;; wayland users may need this
  :stacking "fg"
  :windowtype "normal"
  :wm-ignore true
  :geometry (geometry :width "10%" :height "10%")
  (label :text "${hello}"))

